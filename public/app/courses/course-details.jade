h2.display-4 {{course.title}}
hr
.row
  .col-md-6
    h3.text-muted.font-italic(ng-show="course.featured") Featured
    h3 Published on {{course.published | date}}
  .col-md-6
    .card
      .card-header Tags
      .card-body
        div(ng-repeat="tag in course.tags") {{tag}}
