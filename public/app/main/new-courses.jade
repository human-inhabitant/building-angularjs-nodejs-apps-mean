.card.bg-light.mb-4
  .card-header New Courses
  .card-body
    .row(ng-repeat="course in courses | orderBy:'published':true | limitTo:10")
      .col-3 {{course.published | date:'MMM d'}}
      .col-9 {{course.title}}
