.card.bg-light.mb-4
  .card-header Featured Courses
  .card-body
    .row(ng-repeat="course in courses | filter:{ featured: true }")
      .col-12
        a(href="/courses/{{course._id}}") {{course.title}}
